import React from "react";
import ReactDOM from "react-dom/client";
const temp = React.createElement(
        "h1",{},"Hello World React"
    )
    let root = ReactDOM.createRoot(document.getElementById('root'))
    root.render(temp)